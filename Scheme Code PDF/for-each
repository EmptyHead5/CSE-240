(for-each (lambda (item)
            ; 这里是对每个元素执行的操作
          )
          list) 
        这是 scheme 语言的for-each方法的标准形式
        第一个变量是一个过程（通常记作为lambda 类型），定义对列表内的每个元素执行的操作。
        第二个变量是一个list，赋值给变量item，之后再for-each内的操作进行代码的运行
        “for-each "是Scheme语言中函数式编程范式的一个很好的例子，它允许您以一种简洁而强大的方式处理数据集合。
